import { Checkbox, FormControlLabel, FormGroup } from '@mui/material';

export const Checkboxes = () => {
  return (
    <div>
    <FormControlLabel
        label = ''
        control={<Checkbox />}
    />
    </div>
  );
};